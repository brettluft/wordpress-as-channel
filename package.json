{
  "name": "wordpress-as-channel",
  "version": "0.1.0",
  "private": true,
  "description": "WordPress as an OpenClaw channel — AI agent collaboration in the block editor",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces --if-present"
  }
}
